[phases.setup]
nixPkgs = ["nodejs_20", "mongodb-tools"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run prisma:generate && npm run build"]

[start]
cmd = "npm run prisma:migrate && npm start"